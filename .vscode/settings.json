{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "telega",
            "database": "${workspaceFolder:telegrambot}/db.db"
        }
    ]
}